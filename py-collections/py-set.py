# set >>> unique elements, not duplicates, not indexed, fast lookup
# set() but this {} is empty dict, add(), update(), remove, discard, pop, clear
# is subset(), superset()

# SET
# Search       O(1) average
# Insert       O(1) average
# Delete       O(1) average
# No indexing

nums = {1,2,3,4,5}

# Union a | b  or a.union(b)
# intersection a & b
# difference a - b
# symmetic defference a ^ b 



